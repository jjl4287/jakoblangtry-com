{
  "name": "jakoblangtry-com",
  "version": "1.0.0",
  "description": "Jakob Langtry's personal website",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "mkdir -p public && cp index.html script.js styles.css public/ && ([ -f 404.html ] && cp 404.html public/) && node scripts/generate-env.js && echo 'Build finished.'"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
} 